getInstance() 함수는 생성자를 호출하는 함수이기 때문에 호출 횟수만큼 객체가 생성되어야 하지만 내부에
조건문을 통해 하나의 객체라도 생성되어있다면 생성자가 호출되는 횟수는 1로 고정
싱글톤은 DB 연결할 때 씀
특징으로는 하나의 객체만 존재함
전역 접근법 클래스명.함수명();
객체의 생명 주기 관리 다른 객체들이 객체를 생성하거나 폐기할 수 없음
장점 : 메모리 절약, 전역적 접근, 일관성 유지
단점 : 테스트, 의존성 추적 어려움

# Generic
## 개념 및 정의
- 클래스나 함수가 다룰 데이터 타입을 미리 명시하여 재사용성과 유연성을 높이는 기술

### 타입 매개변수
- 클래스나 함수에서 사용할 실제 타입을 대신하여 사용하는 매개변수
- 일반적으로 알파벳 대문자 하나로 표기함
  1. T(Type) : 일반적으로 클래스 타입을 나타낼 때 사용
  2. E(Element) : 컬렉션의 요소를 나타낼 때 사용
  3. K(Key) : Map의 Key를 나타낼 때 사용
  4. V(Value) : Map의 Value 또는 Generic 타입의 리턴 값으로 나타낼 때 사용

### 재사용성
- Generic을 사용하면 다양한 자료형에 대해 하나의 클래스나 함수를 정의할 수 있어 코드의 재사용성을 높이고 중복을 줄임
```java
package ch18_generic.bmi;

public class PersonMain {
    public static void main(String[] args) {
        Person person = new Person("김일", 169, 64);
        Bmi bmi1 = new Bmi();
        bmi1.printResult(person);
        
        Dog dog1 = new Dog("강이지", 132, 64);
        bmi1.printResult(dog1); // 에러 뜸
    }
}

```

# 오늘 배운것
1. 싱글톤
2. 제네릭
